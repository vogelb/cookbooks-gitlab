#
# PRODUCTION
#
production:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: <%= @database %>
  pool: 5
  username: root
  password: <%= node['mysql']['server_root_password'] %>
  # host: localhost
  # socket: /tmp/mysql.sock

